# tasks 2026-08-19 — ตรวจสอบ purge คลิปวิดีโอใน GCS

## คำถามจาก ท่านต้น
"ทำไมใน storage GCP ยังมีข้อมูลอยู่ ทั้ง ๆ ที่สั่งให้ purge ทิ้งทุก ๆ 3 วัน"

## ผลตรวจ (live, gcloud auth = piyaphan.po@gmail.com)

| ระบบ | สถานะ purge 3 วัน |
|---|---|
| Firestore `sessions.expireAt` (DB `nonprd`) | ✅ TTL **ACTIVE** |
| Firestore `shots.expireAt` | ✅ TTL **ACTIVE** |
| GCS `gs://adge-tennis-nonprd-clips` | ❌ **ไม่มี lifecycle rule เลย** |

คำสั่งที่ใช้ตรวจ:
```bash
gcloud storage buckets describe gs://adge-tennis-nonprd-clips \
  --project adge-tennis-nonprd --format="json(name,lifecycle_config)"
# → { "name": "adge-tennis-nonprd-clips" }  ← ไม่มี lifecycle_config

gcloud firestore fields ttls list --database=nonprd \
  --collection-group=sessions --project adge-tennis-nonprd
# → …/fields/expireAt  ACTIVE   (shots ก็ ACTIVE เหมือนกัน)
```

## Root cause

ตอน migrate `ton-team` → `adge-tennis-nonprd` (2026-07-20) bucket ใหม่ `adge-tennis-nonprd-clips`
ถูกสร้างขึ้น **แต่ไม่ได้ตั้ง lifecycle rule ตามมา** — rule 3 วันเดิม (ตั้งไว้ตอน v0.5, 2026-07-05)
อยู่บน bucket เก่า `ton-phet-clips` ซึ่งถูกลบไปพร้อมโปรเจค `ton-team`

ที่ไม่มีใครจับได้ เพราะ:
- เอกสาร (`HANDOFF.md` ตาราง GCP + `server/gcs.mjs:4` + `db.mjs:11` + `lib.mjs:23` + `api.ts:295`)
  เขียนต่อ ๆ กันมาว่า "bucket lifecycle 3 วัน" โดยไม่มีใครไปตรวจของจริงหลัง migration
- โค้ดฝั่ง server **ไม่ลบไฟล์ GCS เลยโดยเจตนา** (`gcs.mjs`: "server NEVER deletes GCS objects")
  → พึ่ง lifecycle 100% พอ rule หาย = ไม่มีอะไรลบเลย

## ของที่ค้างอยู่ (ณ 2026-08-19)

- **164 objects · 62.06 MiB** (mp4 คลิปสวิง + wav เสียงโค้ช)
- เก่าสุด `2026-07-20T09:53:41Z` (อายุ 30 วัน) · ใหม่สุด `2026-07-28T03:51:39Z`
- **ทุกไฟล์เป็น orphan** — metadata Firestore หมดอายุไปตั้งแต่ 3 วันหลังบันทึกแล้ว
  (leaderboard_records/usage_records ไม่ได้อ้าง clipPath) เปิดดูจากแอปไม่ได้อยู่แล้ว
- ค่าเก็บ ~62 MiB ≈ 0.05 บาท/เดือน (เล็กมาก แต่โตเรื่อย ๆ ถ้าไม่ตั้ง rule)

## PO decision (ท่านต้น, 2026-08-19)

**"เก็บไว้ใน backlog"** — ยังไม่ตั้ง lifecycle rule ตอนนี้ ไม่ลบไฟล์เก่า

## วิธีแก้ (พร้อมรันเมื่อ ท่านต้น สั่ง)

```bash
echo '{"rule":[{"action":{"type":"Delete"},"condition":{"age":3}}]}' > /tmp/lc.json
gcloud storage buckets update gs://adge-tennis-nonprd-clips \
  --lifecycle-file=/tmp/lc.json --project adge-tennis-nonprd
```

⚠️ ตั้งแล้ว GCP จะลบไฟล์ที่อายุ >3 วัน **ทั้ง 164 ตัวภายใน ~24 ชม.** — ลบจริง กู้ไม่ได้
(bucket ไม่ได้เปิด versioning). lifecycle ไม่ใช่ realtime — รันวันละครั้ง, นับจาก object creation time

⚠️ **บทเรียน:** ตอน migrate ไป `adge-tennis-prod` ต้องตั้ง 3 อย่างนี้ที่ไม่ได้อยู่ในโค้ด:
(1) GCS lifecycle 3 วัน (2) Firestore TTL `expireAt` บน sessions+shots
(3) Firestore index `shots.id` COLLECTION_GROUP

## งานอื่นในรอบนี้

- ตอบ progress ล่าสุดของโครงการ + อธิบายระบบ Room Control 4 ห้อง/4 คอร์ท
- verify URL ทั้ง 5 service ของ Room Control ด้วย curl สด (control `/login` 302→200, room1-4 `/api/config` ตอบ roomId ตรง)
- อัปเดต memory `end-of-task-send-urls` — Room Control เป็นรายการ**บังคับ**ที่ต้องส่งทุกครั้ง (เคยตกหล่น)
- หมายเหตุ env: `git` ในเครื่องพังจาก xcode-select → ใช้ `DEVELOPER_DIR=/Library/Developer/CommandLineTools git …`
  (แก้ถาวร: `sudo xcode-select --switch /Library/Developer/CommandLineTools`)
