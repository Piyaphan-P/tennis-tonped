# tasks 2026-07-20 — ย้ายทุก reference จาก `ton-team` → `adge-tennis-nonprd` / `adge-tennis-prod`

> คำสั่ง user: "ให้มาใช้ adge-tennis-nonprd (SIT) กับ adge-tennis-prod ทั้งหมด ของเดิม ton-team replace ได้เลย"
> \+ กฎเหล็กใหม่: tasksYYYYMMDD.md ทุกวันทำงาน + อัพเดท HANDOFF.md/CLAUDE.md/git ทุกครั้ง
> \+ ทำไฟล์ HTML สรุปภาพรวม infra/git/CLAUDE.md/tasks/handoff (theme มืด geek tech)

## ค่าจริงบน GCP (ตรวจสดแล้ววันนี้)

- **adge-tennis-nonprd (SIT):** Cloud Run `adge-tennis-sit` + `adge-ranking-sit` · bucket `adge-tennis-nonprd-clips` · AR repo `adge` (image `…/adge-tennis-nonprd/adge/app:sit-v7` deploy อยู่) · secret `gemini-api-key` · Firestore DB `nonprd` (index `shots.id` COLLECTION_GROUP = **READY** — blocker เก่าปิดแล้ว) · runtime SA `sa-adge-tennis-non-prd@adge-tennis-nonprd.iam.gserviceaccount.com`
- **adge-tennis-prod:** โปรเจคว่างเปล่า — Cloud Run/AR/Secret Manager API ยังไม่ enable (ยังไม่มีการ migrate prod)
- `ton-team` ถูกลบไปแล้ว (2026-07)

## Tasks

- [x] 1. `server/liveRelay.mjs` — default `GOOGLE_CLOUD_PROJECT` → `adge-tennis-nonprd`
- [x] 2. `server/dbFirestore.mjs` — default project + comment → `adge-tennis-nonprd`
- [x] 3. `server/gcs.mjs` — default bucket → `adge-tennis-nonprd-clips` + comment
- [x] 4. `package.json` / `server/package.json` (+lock names) — → `adge-tennis` / `adge-tennis-server`
- [x] 5. `README.md` — deploy commands ชี้ `adge-tennis-nonprd` / `adge-tennis-sit`
- [x] 6. `CLAUDE.md` — SIT env section + Deploy section + Firestore env defaults + index status READY + กฎเหล็ก + session log entry
- [x] 7. `HANDOFF.md` — เขียนใหม่ทันปัจจุบัน (เดิมค้าง v1.0 ตั้งแต่ 2026-07-08)
- [x] 8. `gcloud config set project adge-tennis-nonprd`
- [x] 9. typecheck + 286/286 tests + build เขียวครบ
- [x] 10. secret audit + commit + push
- [x] 11. HTML dashboard สรุป infra + git history + CLAUDE.md + tasks/handoff (theme มืด geek tech) → Artifact
- [x] 12. วางแผน UAM v1.5 → `plan-uam-v15.md` — **user อนุมัติแล้ว** + ตอบ 3 ข้อ: (1) leaderboard เห็นหมด (2) Firestore เท่านั้น — prod จะย้ายมา Firestore ด้วย, Postgres = stub (3) admin จัดการ password อย่างเดียว
- [x] 13. **UAM v1.5 implement + deploy + E2E — เสร็จสมบูรณ์** — 2 agents ขนาน (server: `authCore.mjs` scrypt+cookie ต่อคน, ownership ทุก route, `/api/users*`, bootstrap admin / frontend: LoginGate email+password, `AdminScreen`, auth slice, logout) → **310/310 tests** → deploy `app:sit-v8` revision `00003` (env `AUTH_SECRET`+`ADMIN_EMAIL=piyaphan.po@gmail.com`+`ADMIN_PASS`; ถอด `GATE_USER/PASS`) → **E2E ผ่านครบบน service จริง**: no-cookie 401 · admin login/gate · เพิ่ม player · p1 เห็นเฉพาะของตัวเอง (p2 เห็น 0) · ข้าม account GET/DELETE = 404 · admin เห็นหมด+PATCH/DELETE ได้ · player แตะ /api/users = 403 · ลบ player ทดสอบแล้ว
  - บันทึกเหตุการณ์: docker buildx ค้างที่ load metadata 2 รอบ (shell โดน kill ด้วย) → แก้โดย `docker pull node:22-slim` ตรง ๆ ก่อน แล้ว build ผ่าน · admin login ครั้งแรก 401 = race ตอน cold start (bootstrap fire-and-forget ยังเขียนไม่เสร็จ) — retry แล้วผ่าน ไม่ใช่บั๊ก
- [x] 14. **แก้ UI ตาม feedback:** กล่อง email ขาว (theme.css เพิ่ม `input[type=email]`) + ช่องชื่อ default = ชื่อเล่น/อีเมลเต็ม (re-seed เมื่อเปลี่ยนบัญชี) + LoginGate/AdminScreen email input `type=email`→`type=text` (คง inputMode)
- [x] 15. **SIT v1.5.1 shipped** (2 agents ขนาน: server usage_records / frontend cut-features+admin-cost) → 317/317 → deploy `app:sit-v9` **revision `00004`** → verify: bundle ไม่มี type:email, `/api/usage` admin ตอบ 200 · ตัด token field/CostFab/Compare · หน้า Admin cost (usage รวมต่อคน + infra estimate)
- [x] 16. **Code review UAM (8 finder ขนาน + verify)** → report 8 ช่องโหว่ (5 security + 3 correctness/efficiency) — **fix รอ user ยืนยันแยก** (ยังไม่แก้ในรอบนี้)
- [x] 17. Dashboard 6 แท็บ (Infra+Cost / CLAUDE.md / Memory / Tasks+Handoff / Git / Agent Workflow) — publish
- [x] 18. **SIT v1.5.2 shipped — fix ช่องโหว่ทั้ง 8** (2 agent ขนาน server/client → 340 tests → deploy `sit-v10` rev00005 → +TTL 1h `sit-v11` rev00006) · E2E ผ่านครบ: valid login ok, ลบ player แล้ว re-login=401, cookie เก่าหมดฤทธิ์ ≤1ชม., usage admin=200/player=403, ownership ครบ · แก้: AUTH_SECRET boot-guard prod · revoke cookie (cache TTL 1h ตาม PO) · gate 401 เคส deleted · rate-limit XFF rightmost · leaderboard recompute จาก shots · scryptSync→async · findShot ครั้งเดียว · 401 clear auth
- [x] 19. Kanban board แท็บ 04 ใน dashboard (To Do/Doing/Done)
- [x] 20. **v1.6 โหมดเสียง/โค้ช shipped** (`sit-v12` rev00007, 357 tests) — ปุ่มเลือกก่อน session: เสียง 4 (หญิงอ่อนโยน→Aoede/หญิงเข้ม→Kore/ชายเข้ม→Charon/ชายเป็นกันเอง→Puck) × สไตล์โค้ช 4 (ให้กำลังใจ/ดุ hardcore มี guardrail/สุภาพ/เพื่อนซี้) → `settings.voiceTone`+`coachMode`, speechConfig+persona ทั้ง 2 transport, default = เดิม
- [x] 21. **v1.4 mitigation shipped** (369 tests → deploy `sit-v13`) — shoulder EMA (α=0.35) กัน chip กะพริบ (เฉพาะ live display, ไม่แตะ scoring angle) + `settings.speedCorrectionFactor` (default 1.0, clamp 0.5–3.0) ปรับ km/h ได้ใน Settings
- [x] 22. **Ranking repo migrated** (`../tennis_ranking01` commit `9c77200`, 23 tests, push แล้ว) — ton-team → adge-tennis-nonprd
- [x] 23. เติมเครดิต Gemini แล้ว (user) — token minting = 200 ยืนยัน key/billing ปกติ
- [x] 24. **v1.8 Summary stats widget shipped** (397 tests → deploy `sit-v14`) — widget บน SummaryScreen: นาทีที่ตี, total shots ทุก session, avg swing speed, burn cal (MET 5.0 × น้ำหนัก, เพิ่มช่อง `settings.playerWeightKg` default 65), topspin%/backspin%/flat (spin.ts จาก deltaY ข้อมือ, วงสมบูรณ์=ตีโดน) · เก็บใน StoredSession (optional fields, backward-compat) รายเซสชัน+รวม 3 วัน · ปุ่ม share canvas 1080×1920 (statsCardRenderer, two-step reuse shareStory)
- [x] 26. เพิ่มแท็บ Tester ใน dashboard (369→397 tests, 6 หมวด) + ทดสอบครบทุก deploy
- [ ] 25. rotate credential — user action (Supabase console / admin password ใน Admin UI); AQ. เก่าหมดอายุแล้ว, ไม่มี secret ใน git

## ผล / หมายเหตุ

- โค้ดที่แก้เป็น **default fallback เท่านั้น** — service จริงตั้ง env ครบอยู่แล้ว ไม่กระทบ runtime, ไม่ต้อง deploy ด่วน (รอบหน้า tag `sit-v8`)
- Session log เก่าใน CLAUDE.md คงไว้เป็นประวัติศาสตร์ (ไม่แก้ย้อนหลัง)
- **ค้างไป repo อื่น:** `../tennis_ranking01` ยังอ้าง `ton-team` — ต้อง replace แบบเดียวกันวันหลัง
- **ค้าง prod:** `adge-tennis-prod` ต้อง enable APIs + สร้าง infra ก่อน migrate `main`
