# tasks 2026-07-20 — ย้ายทุก reference จาก `ton-team` → `adge-tennis-nonprd` / `adge-tennis-prod`

> คำสั่ง user: "ให้มาใช้ adge-tennis-nonprd (SIT) กับ adge-tennis-prod ทั้งหมด ของเดิม ton-team replace ได้เลย"
> \+ กฎเหล็กใหม่: tasksYYYYMMDD.md ทุกวันทำงาน + อัพเดท HANDOFF.md/CLAUDE.md/git ทุกครั้ง
> \+ ทำไฟล์ HTML สรุปภาพรวม infra/git/CLAUDE.md/tasks/handoff (theme มืด geek tech)

## ค่าจริงบน GCP (ตรวจสดแล้ววันนี้)

- **adge-tennis-nonprd (SIT):** Cloud Run `adge-tennis-sit` + `adge-ranking-sit` · bucket `adge-tennis-nonprd-clips` · AR repo `adge` (image `…/adge-tennis-nonprd/adge/app:sit-v7` deploy อยู่) · secret `gemini-api-key` · Firestore DB `nonprd` (index `shots.id` COLLECTION_GROUP = **READY** — blocker เก่าปิดแล้ว) · runtime SA `sa-adge-tennis-non-prd@adge-tennis-nonprd.iam.gserviceaccount.com`
- **adge-tennis-prod:** โปรเจคว่างเปล่า — Cloud Run/AR/Secret Manager API ยังไม่ enable (ยังไม่มีการ migrate prod)
- `ton-team` ถูกลบไปแล้ว (2026-07)

## Tasks

- [x] 1. `server/liveRelay.mjs` — default `GOOGLE_CLOUD_PROJECT` → `adge-tennis-nonprd`
- [x] 2. `server/dbFirestore.mjs` — default project + comment → `adge-tennis-nonprd`
- [x] 3. `server/gcs.mjs` — default bucket → `adge-tennis-nonprd-clips` + comment
- [x] 4. `package.json` / `server/package.json` (+lock names) — → `adge-tennis` / `adge-tennis-server`
- [x] 5. `README.md` — deploy commands ชี้ `adge-tennis-nonprd` / `adge-tennis-sit`
- [x] 6. `CLAUDE.md` — SIT env section + Deploy section + Firestore env defaults + index status READY + กฎเหล็ก + session log entry
- [x] 7. `HANDOFF.md` — เขียนใหม่ทันปัจจุบัน (เดิมค้าง v1.0 ตั้งแต่ 2026-07-08)
- [x] 8. `gcloud config set project adge-tennis-nonprd`
- [x] 9. typecheck + 286/286 tests + build เขียวครบ
- [x] 10. secret audit + commit + push
- [x] 11. HTML dashboard สรุป infra + git history + CLAUDE.md + tasks/handoff (theme มืด geek tech) → Artifact

## ผล / หมายเหตุ

- โค้ดที่แก้เป็น **default fallback เท่านั้น** — service จริงตั้ง env ครบอยู่แล้ว ไม่กระทบ runtime, ไม่ต้อง deploy ด่วน (รอบหน้า tag `sit-v8`)
- Session log เก่าใน CLAUDE.md คงไว้เป็นประวัติศาสตร์ (ไม่แก้ย้อนหลัง)
- **ค้างไป repo อื่น:** `../tennis_ranking01` ยังอ้าง `ton-team` — ต้อง replace แบบเดียวกันวันหลัง
- **ค้าง prod:** `adge-tennis-prod` ต้อง enable APIs + สร้าง infra ก่อน migrate `main`
