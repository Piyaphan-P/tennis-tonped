# tasks 2026-07-24 — SIT v2.0 (coach verbosity axis)

Branch `SIT` · ADGE Tennis (non-prod)

## เป้าหมายวันนี้
มีคอมเมนต์บนสนามว่า **โค้ชพูดยาวเกินไป** → เพิ่มฟีเจอร์ให้ผู้เล่นเลือก **ระดับความกระชับของคำโค้ช (สั้น/กลาง/ยาว)** ในหน้า Home ก่อนเริ่ม session (แกนที่ 3 ต่อจาก voice tone + coach mode ของ v1.6)

## PO decision
- default = **สั้น (short)** — ตอบ feedback ทันที (คนที่ไม่กดเลือกได้ short เลย)

## งานที่ทำ (v2.0) — ✅ เสร็จ, gate เขียวครบ
- [x] `types.ts` — `type Verbosity = 'short'|'medium'|'long'` + field `verbosity` ใน `Settings`
- [x] `store.ts` — `LS_VERBOSITY='tp.verbosity'`, `VERBOSITY_LEVELS`, `readEnum(... ,'short')` (default short), setter `setVerbosity` (persist)
- [x] `liveClient.ts` — แกน length ที่เคย hardcode 5 จุดถอดออกหมด:
  - `LENGTH_CLAUSE` (const) → `LENGTH_BANDS` + `lengthClause(verbosity)` (pure, exported)
  - ถอด length ออกจาก **30 style directive** (directive = STRUCTURE ล้วน) → `buildShotPrompt` ต่อ `lengthClause` ครั้งเดียวท้าย prompt
  - `COACH_SYSTEM_PROMPT` ใส่ placeholder `{{LENGTH}}` + reword mandate ให้ conditional: ที่ short ให้เหลือแค่ชื่อช็อต + 1 beat (praise/cue merge/drop ได้), ที่ medium/long เต็มรูป
  - reword anti-variety guardrail: length = fixed **ต่อ session** (ไม่ใช่ shot-to-shot) — ไม่ reopen ปัญหา v1.4
  - `buildCoachSystemPrompt` + `buildShotPrompt` รับ verbosity (function default `medium` = ข้อความเดิมเป๊ะ เพื่อ back-compat pure callers/tests)
  - connect site + dispatch site ส่ง `settings.verbosity` **ทั้งสองจุด** (system prompt กับ per-shot ต้องตรงกัน — advisor เตือน)
- [x] `HomeScreen.tsx` — การ์ดที่ 3 "ความยาวคำโค้ช" (โครงเดียวกับการ์ด voice tone) + hint
- [x] `i18n.ts` — `home.verbosity.title/short/medium/long/hint`
- [x] tests — `lengthClause` 3 band ต่างกัน · medium = ข้อความเดิม · short permit merge/drop · **opener รอดที่ short** · buildShotPrompt/SystemPrompt inject band · store slice (default short + persist + ไม่กวน voice/mode)

## Gate
- typecheck ✅ · **test 414/414 ✅** (403 → +11) · build ✅ · secret audit ✅ clean

## หลักการที่ยึด (advisor)
- length ฝังจริง 5 จุด ไม่ใช่ 3 — grep `2 to 4 / 2–4 / 4–9` reconcile หมด
- short ชนกับ mandate "ชื่อช็อต+ชม+fix+cue เสมอ" → ทำ mandate ให้ conditional ตาม verbosity ไม่งั้นโมเดลได้คำสั่งขัดกัน (opener คงไว้เสมอ = load-bearing UX)
- verbosity ไม่ขึ้น wire — ไหลผ่านแค่ prompt text ที่ build ฝั่ง browser (เหมือน coachMode) → **ไม่แตะ server**

## งานเพิ่ม (บ่ายวันเดียวกัน) — ✅ ซ่อน section ราคาสำหรับ player
- user: "หน้า player ตรงตั้งค่ามีให้กรอก token — เอาออกสำหรับ player"
- สอบแล้ว: input token จริงถอดไป 2026-07-20 แล้ว → สิ่งที่ player เห็น = section **"ราคา (USD ต่อ 1M โทเคน)"** (6 ช่องกรอกเลข พาดหัวมีคำว่า "โทเคน") = ของ dev/admin, v1.5.1 ซ่อน cost ฝั่ง player ไปแล้วแต่ลืม section นี้
- แก้: `SettingsSheet.tsx` gate block ราคาไว้หลัง `auth?.role === 'admin'` (rates ยังป้อน costMonitor จาก store default เหมือนเดิม ไม่พึ่ง UI นี้)
- typecheck ✅ · test 414/414 ✅ · build ✅ · secret clean ✅

## ยังไม่ได้ทำ / ถัดไป
- [ ] **ยังไม่ build/deploy image** — โค้ดอยู่ใน git แล้ว รอ user สั่ง deploy (docker local + colima)
- [ ] long (~16s) สู้กับ pacing speak-to-completion (v0.7) — reply ยาวขึ้น queue จะ drop ช็อตมากขึ้น (เป็น trade-off ที่ผู้ใช้เลือกเอง) → เทสสนามจริง
- [ ] เทสสนาม: short สั้นพอ/ยังได้ยินชื่อช็อตไหม · long ยาวเกินไปไหม
