# tasks 2026-07-28 — SIT v2.1 (LINE identity + room login + external history API)

Branch `SIT` · ADGE Tennis (non-prod)

## เป้าหมาย (จาก user)
1. **รับ LINE profile** (`lineUserId`/`displayName`/`pictureUrl`/`email`) — ปุ่มสแกน QR (payload=JSON) + tab กรอกเอง
2. **External API** ดึงประวัติ+stats+ลิงก์วิดีโอ query ด้วย lineUserId/email + ต้องมี **API auth key**
3. **Room login** — เปลี่ยน email login → `roomUser`/`roomPassword` (ห้อง 1,2,3,4), เปลี่ยนชื่อ field ใน DB, ลบของเก่าได้
4. player email เก็บเป็น `lineEmail`
5. DESIGN.md แยก coach / ranking (ไป Stitch)

## PO decisions
- LINE profile อยู่หน้า Home ก่อน session (per-player) · API key = single env `HISTORY_API_KEY` (x-api-key) · video = proxy URL ที่ต้องใช้ key · ประวัติ = เท่าที่มีจริง (3วัน + stats ถาวร) · cost ไม่โชว์ใน ext API
- room login = แทน email ทั้งระบบ · admin = roomUser พิเศษ (env ADMIN_USER/ADMIN_PASS)

## ⚠️ Identity model
- `roomUser` = บัญชีห้อง (แชร์, ไม่ใช่ตัวผู้เล่น) — เดิม ownerEmail
- `lineUserId` = key ต่อ session ของผู้เล่นแต่ละคน · `lineEmail` = email ส่วนตัว (lowercased)
- ไม่มี backfill: ext API เห็นเฉพาะ session หลังผูก profile

## งานที่ทำ — ✅ gate เขียว (typecheck ✓ · test 434 ✓ · build ✓ · secret clean)

### Part A — LINE profile capture
- `types.ts` LineProfile + Settings.lineProfile + CloudSessionSummary.{lineUserId,lineEmail}
- `src/line/parseQr.ts` (pure, tolerant casing `LineuserId`, lowercase email) + 8 tests
- `store.ts` LS `tp.lineProfile`, `setLineProfile` (persist + seed userName) + 5 tests
- `src/components/LineProfileSheet.tsx` — 2 tab: สแกน QR (getUserMedia + `jsqr`, downscale ≤320px, `track.stop()` on close/tab-away/unmount) | กรอกเอง; preview การ์ด avatar+ชื่อ
- `HomeScreen.tsx` การ์ด "ผู้เล่น (LINE)" (avatar/ชื่อ + ปุ่มเปลี่ยน/ระบุ) + sheet
- `package.json` +`jsqr` · i18n `line.*`/`home.player.*`
- `api.ts createSession` + `cloudSync.ts` ส่ง lineUserId/lineEmail ตอนสร้าง session

### Part B — persist (backend)
- `dbFirestore.createSession` เก็บ roomUser/lineUserId/lineEmail; leaderboard upsert copy line identity จาก session doc
- `listHistoryByLine`/`getStatsByLine` (equality-where, **ไม่ orderBy**) + pg stub 503
- `lib.mjs` sessionDocToJson +line fields; `leaderboardDocToJson`+`foldLeaderboardStats` (pure) +4 tests

### Part C — external API
- `server/extApi.mjs` — `/api/ext/{history,stats,clips/:id,audio/:id}`; `apiKeyMatches` (ย้ายไป authCore = testable, constant-time) +3 tests; reuse streamClip Range/206
- `authGate.mjs` guard skip `/ext` prefix (เฉพาะ ext) · `index.mjs` mount + 503 เมื่อไม่มี key

### Part D — ROOM login rename (UAM v2.1)
- `authCore.mjs` isValidEmail→+isValidRoomUser (`^[a-z0-9._-]{2,40}$`); cookie `roomUser|role|exp`; evaluateGuard roomUser; +isValidRoomUser/apiKeyMatches tests
- `authGate.mjs` login by roomUser, req.user.roomUser, cache/limiter keys
- `dbFirestore.mjs` users/{roomUser}, ensureAdmin({roomUser}); ownerEmail→roomUser ทุกที่
- `db.mjs` pg stub roomUser · `lib.mjs` userDocToJson/aggregateUsageRows roomUser · sessionRowToJson +line null
- `routes.mjs` canAccess/POST sessions/history `?room=`/users CRUD `:roomUser` · `index.mjs` ADMIN_USER (❌ ไม่ fallback ADMIN_EMAIL — email fail regex = lockout)
- FE: types AuthUser.roomUser/AdminUserRow.roomUser/CloudSessionSummary.roomUser; store setAuth; api login(roomUser)/userOf/CreateUserInput/UsageUserRow; LoginGate room field; AdminScreen roomUser+ROOM_USER_RE; SettingsSheet; playerStats roomUser; i18n login.room
- tests updated: authCore/lib/store.auth/api.auth401/playerStats

### DESIGN.md (agent) — coach `DESIGN.md` + ranking `../tennis_ranking01/DESIGN.md` (Stitch-ready)

## ⚠️ advisor blockers (แก้แล้ว)
1. **ADMIN_EMAIL fallback = total lockout** (email fail isValidRoomUser) → ตัด fallback, ต้อง set `ADMIN_USER=admin` ตอน deploy
2. server `.mjs` ไม่ถูก typecheck → grep sweep + node --check ทุกไฟล์ ✓; liveRelay ใช้แค่ isGateAuthorized (non-null) — live coach ปลอดภัย
3. `HISTORY_API_KEY` ต้อง set (Secret Manager) ไม่งั้น /api/ext/* 503

## Deploy — ✅ DEPLOYED (rev `00012`, image `app:sit-v17`)
- secret `history-api-key` สร้าง + grant secretAccessor ให้ runtime SA
- deploy `--update-env-vars ADMIN_USER=admin --update-secrets HISTORY_API_KEY=history-api-key:latest`
- **E2E live ✓:** app / = 200 · gate no-cookie = 401 · ext no-key/bad-key = 401 · **ext good-key = 200** (history+stats JSON ถูก) · **admin login `admin` = 200 role=admin** (ไม่ lockout)
- admin login ใหม่ = `admin` / (ADMIN_PASS เดิม); ทุก device หลุด (cookie เก่า roomUser ไม่มี)
- HISTORY_API_KEY เก็บใน Secret Manager (ให้ user ดึงจาก secret ตอนเรียก API)

## เปิดค้าง (ถาม user)
- **hard-gate?** user บอก player "ต้องสแกนบอกว่าเป็นใคร" ก่อนเริ่ม — ตอนนี้เป็นการ์ด optional; จะ block ปุ่มเริ่มถ้ายังไม่ผูก profile ไหม?
- Firestore composite index? ไม่ต้อง (equality-where อย่างเดียว)

---

## เพิ่มเติม (เย็น-ดึก 2026-07-28)

### v2.2 — scale-invariant shot detection (แก้มือถือจับช็อตไม่ค่อยได้) ✅ deployed
- root cause: wristSpeed = hypot(dx,dy)/dt บน normalized frame coords → ผู้เล่นเล็กในเฟรม (มือถือไกล) speed ต่ำ ไม่ถึง gate
- fix: หารด้วยความยาวลำตัว (nose→ankle, smoothed EMA 0.1 + carry last-good) → body-lengths/s scale-invariant; gate ×1.8; SPEED_GOOD 1.1→2.0 (drift-lock); km/h reconcile (ไม่ double-divide — advisor จับ)
- `settings.captureSensitivity` (0.3–2.0 default 1.0) ปรับ gate สดใน Settings ไม่ต้อง redeploy
- ย้ายปุ่ม จบ session + สลับกล้อง ขึ้นบน (กันโดนบังบนมือถือ)
- +3 tests (scale-invariance + knob), 437 green

### v2.3 — auto-save + coach cue in History ✅ deployed (`app:sit-v18` rev 00013)
- auto-save: periodic 20s + page-hide → syncSessionEnded (cloud, idempotent) + snapshotSessionToHistory (local, upsert by stable id) → session ขึ้น History/ranking แม้ไม่กดจบ
- coach cue: เก็บข้อความโค้ชต่อช็อต (`shot.coachText` via PATCH /api/shots/:id/coach) → โชว์ block "โค้ชแนะนำ" ในหน้า History
- E2E live: root 200 · ext 200 · coach route 401 (guarded)

### Admin Portal (แยกแอป `../tennis_admin_portal`, own git) — โค้ดเสร็จ, ยังไม่ deploy
- 3 หน้า: Dashboard (usage 4 สนาม day/week/month + date picker) · All Sessions (map session→LINE player) · Player & LINE mgmt
- Google Sign-In (GIS) allowlist piyaphan.po@gmail.com · ต่อ Firestore nonprd จริง (leaderboard⋈sessions, player_assignments) + mock fallback
- ⏳ รอ OAuth Client ID จาก ท่านต้น → deploy (ยังไม่ deploy เพราะ fail-open+DB จริง = ข้อมูลหลุด)
- go-live: build --build-arg VITE_GOOGLE_CLIENT_ID=… + env GOOGLE_CLIENT_ID/ALLOWED_EMAILS/SESSION_SECRET/GOOGLE_CLOUD_PROJECT/FIRESTORE_DATABASE + grant SA roles/datastore.user + deploy adge-admin-portal-sit

---

# tasks 2026-07-28 (เพิ่ม) — SIT v2.4 (dev-plan share fix + history dev-plan + ext devplan API)

## เป้าหมาย (จาก ท่านต้น)
1. **บั๊ก (ก่อนเลย):** widget "แชร์สรุปวันนี้" ในหน้าแผนพัฒนา โชว์รูป player (ภาพสวิง) แทนที่จะเป็น "แผนพัฒนา"
2. เอา **สรุป + แผนพัฒนา** ใส่ในประวัติของแต่ละ session (History detail)
3. External API เพิ่มเส้นดู **สรุปแผนพัฒนา + สรุป**

## รากของบั๊ก
- ปุ่ม `devplan.shareSummary` ("แชร์สรุปวันนี้") ผูกกับ `StoryShareButton` → `storyRenderer` ซึ่งเป็นการ์ด "สตอรี่ต่อสวิง" (hero = ภาพ/คลิปสวิง `drawImage`) — เลยเห็นเป็นรูป player. ไม่มี devPlan renderer มาก่อน. LINE `pictureUrl` ไม่เคยถูกวาดลง canvas เลย (grep ยืนยัน)

## งานที่ทำ — ✅ gate เขียว (typecheck ✓ · test 452 ✓ · build ✓ · secret clean)

### Part 1 — แก้บั๊ก share
- `src/history/devPlanDerive.ts` (ใหม่, pure) — `rankAreas`/`areaForIssue`/`SEVERITY_WEIGHT` ดึงออกจาก DevPlanScreen (แชร์กับ History) + 6 tests
- `src/share/devPlanRenderer.ts` (ใหม่) — การ์ด 1080×1920 วาด "แผนพัฒนา" จริง (area + อาการ/วิธีซ้อม/cue) ไม่มีภาพสวิง; empty areas → การ์ด "ฟอร์มดี" เชิงบวก (ไม่ว่างเปล่า) + 4 tests
- `src/components/DevPlanShareButton.tsx` (ใหม่) — two-step activation-safe (เหมือน StatsShareButton) reuse `shareStory`/`saveSwingVideo`
- `DevPlanScreen.tsx` — สลับปุ่ม `devplan.shareSummary` เป็น `DevPlanShareButton`, gate เป็น `hasSession` (ไม่ใช่ bestShot) · ปุ่มแชร์ต่อ-miss-clip (`devplan.shareStory`) คงเดิม (แชร์คลิปสวิงถูกต้องแล้ว) · refactor ใช้ shared ranker

### Part 2 — History detail
- `HistoryScreen.tsx` — เพิ่ม `DevPlanBlock` (derive จาก `shots[].issues` ผ่าน `rankAreas`, ไม่ใช่ `detail.summary` → auto-save session ที่ summary=null ก็ได้แผน) หลังการ์ดสรุป; render nothing ถ้าไม่มี fault; import `devplan.css`

### Part 3 — ext devplan API (แยก service `adge-stat-api-sit`)
- `server/lib.mjs` — `deriveDevPlan(shots)` + `devPlanAreaForIssue` (mirror ของ frontend, pure) + 3 tests
- `server/extApi.mjs` — `GET /api/ext/devplan?lineUserId=|email=` คืน per-session `{summary, devPlan:{areas:[{id,weight,shots}]}}` — ไม่ fork copy (อาการ/วิธีซ้อม/cue อยู่ i18n; summary.improvements bilingual อยู่แล้ว)

## Deploy
- coach `app:sit-v21` (parts 1+2) · stat-api `stat-api:v2` (part 3) — ต้อง deploy สองตัว

---

# tasks 2026-07-28 (เพิ่ม) — SIT v2.5 (History detail: stats widget + share ×2 + ext stats)

## เป้าหมาย (จาก ท่านต้น)
1. หน้าประวัติ (History detail) ยังไม่มี **ปุ่มแชร์แผนพัฒนา**
2. ขาด **การ์ดสรุปแบบหลังจบ session** (แคลอรี/ความเร็ววงสวิง/สปิน) — และต้อง **แชร์ได้**
3. แก้ **external API** ให้ดึงสถิติพวกนี้ได้ + curl เช็คตอนจบ

## วิธีทำ (Fable plan → Opus + Sonnet)
- สถิติ (≈ความเร็ว/≈kcal/สปิน) เดิมมีแค่ live (SummaryScreen) เพราะ CloudShot ไม่มี captures/speed → **persist ลง `summary`** (verbatim round-trip ทั้ง Firestore + ext API, **ไม่ต้องแก้ storage**)

## งานที่ทำ — ✅ gate เขียว (typecheck ✓ · test 468 ✓ · build ✓ · Fable SHIP)
- **Opus (data):** `types.ts` +`SessionSummaryJson.{avgSpeedKmh?,kcal?,spin?}` (optional, back-compat) · `cloudSync.syncSessionEnded` compute `deriveSessionStats` ใส่ทั้ง final + provisional flush · `src/history/detailStats.ts` (pure resolver: summary→local StoredSession→recompute, `matchLocalSession` playerKey+±120s+closest) +tests · `server/lib.sessionStatsFromSummary` (safe defaults, import-free) + wire เข้า `/api/ext/history` + `/devplan` เป็น field `stats` +tests
- **Sonnet (UI):** `HistoryScreen` การ์ด stats widget (นาที/ช็อต/≈ความเร็ว/≈kcal + แถบสปิน%) เหนือการ์ดสรุป + `StatsShareButton` (gate shotCount>0, cum fallback = ค่าตัวเองเมื่อไม่มี local) + `DevPlanShareButton` บน DevPlanBlock (thread playerName/dateLabel, useMemo เหนือ early-return กัน hooks bug) · ไม่ต้องเพิ่ม i18n key

## Deploy
- coach `app:sit-v22` (frontend) · stat-api `stat-api:v3` (ext stats) — deploy สองตัว + curl ext เช็ค

---

# tasks 2026-07-28 (เพิ่ม) — v2.6 เซฟ session ว่าง + Google login/whitelist (cross-app)

## v2.6 — เซฟทุก session ลงประวัติแม้ 0 ช็อต (coach app)
จาก ท่านต้น: "สร้าง session แล้วเซฟลงประวัติทุกครั้ง แม้ไม่มีการตี คะแนน 0 stat 0"
- ลบ guard `≥1 ช็อต` ใน `store.snapshotSessionToHistory`/`endSession` + `cloudSync.autoSaveFlush` → เหลือ gate `startedAtMs>0`
- `cloudSync.syncSessionEnded` สร้าง cloud session ให้ empty (ensureSession) → เข้า cloud history ด้วย
- leaderboard ไม่กระทบ (server คิดจากช็อตจริง = 0 ช็อต → ไม่มีแถว ranking)
- `app:sit-v24` rev 00019 · +4 tests (store.emptySession) · 472 tests

## Google login + email whitelist (Admin Portal + Room Control control)
จาก ท่านต้น: "control login google piyaphan.po@gmail.com + admin portal เพิ่ม email whitelist คุม login ทั้ง control + portal"
- Firestore `login_allowlist/{email}` ใช้ร่วม 2 แอป · `isAllowedAsync` = super-admin OR ALLOWED_EMAILS env OR collection (cache 60s, fail-safe floor)
- super-admin (piyaphan.po@gmail.com) allowed เสมอ + คนเดียวที่แก้ whitelist ได้ (กัน lockout)
- **Admin Portal** (`admin-portal:v2` rev 00002): AllowlistScreen (super-admin) + CRUD + `/me` มี isSuperAdmin
- **Room Control** control (`room-control:v5` rev 00005): Google Sign-In (authGoogle.mjs, cookie `adge_control`), command route ต้องมี cookie เมื่อ GOOGLE_CLIENT_ID set · **rooms 1-4 ไม่แตะ**
- verify: control /me 401, command 401, config authConfigured true · portal /me 401 · room heartbeat ok
- ⚠️ **ต้องเพิ่ม control URL ใน OAuth Authorized JavaScript origins** (Google console) ไม่งั้นปุ่ม login error

## เพิ่ม — Main Control 2-page login + Ranking Google login (ด่วน)
จาก ท่านต้น: "login control เสร็จให้เปิดอีกหน้า ไม่ใช่ไถลงมา · ranking ไม่มี google login · ทำด่วน"
- **Main Control** (`room-control:v6` rev 00006): แยกหน้า `/login` (login.html/js) → login สำเร็จ navigate ไป `/control` (คนละหน้า); `/control` เด้งกลับ `/login` ถ้าไม่ auth; `/` → `/login`. verify live: / → /login ✓
- **Ranking** (`ranking:sit-v3` rev 00002, React/Vite): เพิ่ม Google login gate (authGoogle.mjs reuse dbFirestore.getDb, cookie `adge_rank`, LoginGate wrap App, /api/leaderboard 401 ไม่มี cookie). verify live: /me 401, /api/leaderboard 401, config authConfigured true, root 200 ✓
- ⚠️ ต้องเพิ่ม **control URL + ranking URL** ใน OAuth Authorized JavaScript origins
- 4 auth cookies ในระบบ: adge_auth (coach) / adge_admin (portal) / adge_control / adge_rank
