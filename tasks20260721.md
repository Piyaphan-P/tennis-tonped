# tasks 2026-07-21

## งานวันนี้: SIT v1.9 — แยกประวัติ/สถิติรายผู้เล่น + admin daily overview

**ปัญหาที่ user รายงาน:** ประวัติหน้าหลักรวม session ของทุก player ปนกัน (เครื่องเดียวกันเล่นหลายคน → "สถิติของคุณ" กลายเป็นของทุกคนรวมกัน)

**Root cause:** history เก็บใน localStorage `tp.history` เป็นของ "เครื่อง" ไม่ใช่ของ "คน" — `StatsCard`/`selectUserStats`/`deriveCumulativeStats` ยำรวมทุก `StoredSession` โดยไม่กรอง `userName`

- [x] `src/history/playerStats.ts` (pure ใหม่): `playerKey`/`filterHistoryByPlayer` (identity = userName trim+lowercase), `derivePlayerBreakdown` (นับ session/shots/avg แยกรายคน), `deriveAdminDaily` (group ตามวัน local + รายชั่วโมง นับผู้เล่นไม่ซ้ำ) + tests 6 ตัว (tz-independent)
- [x] `selectUserStats` (store.ts) กรองเป็นของ player ปัจจุบันเท่านั้น → StatsCard + SummaryScreen
- [x] `HistoryList` (Home): บล็อก "จำนวนตีแยกรายคน (3 วันล่าสุด)" + ชื่อคนเล่นบนทุกแถว session + sparkline เฉพาะ player ปัจจุบัน
- [x] `StatsCard`: หัวการ์ดต่อท้ายชื่อ player
- [x] `SummaryScreen`: cumulative ("รวมทุกครั้ง") + history list ท้ายจอ ใช้เฉพาะ session ของ player ปัจจุบัน
- [x] `AdminDailyStats.tsx` (ใหม่): login เป็น admin → หน้า Home แสดง "สถิติรวมรายวัน" แทน widget ส่วนตัว — ต่อวัน: ผู้เล่นกี่คน/กี่เซสชัน/กี่ช็อต + แถบรายชั่วโมง (นับผู้เล่นไม่ซ้ำจาก `/api/history` ที่ admin เห็นทุกคนอยู่แล้ว)
- [x] i18n TH/EN: `history.perPlayer/unnamed/you`, `adminDaily.*`
- [x] typecheck ✓ · 403/403 tests ✓ · build ✓
- [x] deploy image `app:sit-v15` → revision `adge-tennis-sit-00010-fxj` + smoke 200
- [x] commit `cde6d48` + push (secret audit clean) + HANDOFF/CLAUDE.md/dashboard อัพเดทแล้ว

**หมายเหตุ design:** identity ฝั่ง player = `userName` (free-text เดียวกับ leaderboard) เพราะสโมสรใช้ login ร่วมเครื่องเดียวแล้วพิมพ์ชื่อเปลี่ยนคน — ไม่ใช่ ownerEmail. ฝั่ง admin ใช้ userName ก่อน, fallback ownerEmail. Server ไม่ต้องแก้ (`/api/history` กรอง owner ถูกอยู่แล้ว; admin เห็นทุกคน)
